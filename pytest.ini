[pytest]
# csms.py and pipeline.py sit at the repo root, and the tests import them
# directly. `rootdir` insertion (importmode=prepend + no __init__.py in tests/)
# only puts tests/ on sys.path, so name the root explicitly — otherwise a bare
# `pytest` works only when invoked as `python -m pytest`.
pythonpath = .
testpaths = tests
